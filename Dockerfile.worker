FROM python:3.11-slim

WORKDIR /app

# Copy and install requirements FIRST (before copying worker code)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Now copy the worker code
COPY worker/ ./worker/
COPY .env .

# Run the consumer
CMD ["python", "-m", "worker.consumer"]